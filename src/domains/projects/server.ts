/**
 * Server-only public API of the projects domain (filesystem content
 * loaders). Import from app server components, sitemap and OG images.
 */
export { getProjects, getProject } from "./services/projects";
